namespace FinPlan.Application.Pockets.Queries.GetPockets;

/// <summary>Server-side ordering options for pocket lists.</summary>
public enum PocketSort
{
    NameAsc = 0,
    NameDesc,
    BalanceDesc,
    BalanceAsc,
}
